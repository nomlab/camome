<br>
<p><font size="5"><%= @event.summary %></font></p>
<br>
<p><%= @event.dtstart %> 〜 <%= @event.dtend %></p>
<br>
<% if @clams != nil %>
  <p>関連するメール<p>
  <ul>
  <% @clams.each do |clam| %>
    <li><%= link_to clam.summary, "" %></li>
  <% end %>
  </ul>
<% end %>
<br>
<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
